package ch.epfl.lara.synthesis.kingpong.expression

class TemplateSolver {
  /**
   * The problem is the following:
   * 
   * 1) The user select one or more objects or events in the game.
   * 2) Then for each object, he/she selects a condition which triggers the action.
   * For shape: Position is after/below/above etc.
   * For events: The finger moved, the joystick moves, the accelerometer.
   * For values: Expression such as greater or less than a constant, than another existing value.
   * 3) 
   * 
   */
}